using UnityEngine;

namespace StackAndCollect.MyStackAndCollect.Scripts
{
    public class StackAreaBehaviour : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
